name: build-deploy

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v3

      - name: Build Docker
        run: docker build -t myapp .

      - name: Run container
        run: docker run -d -p 80:80 myapp
